<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D0B08">

    <!-- Face scan preview (revealed when scanning) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/facePreview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_marginTop="90dp"
        android:layout_marginBottom="90dp"
        android:background="#000000"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/lockPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:letterSpacing="0.25"
            android:text="LOCKED"
            android:textColor="#C9A24B"
            android:textSize="13sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:fontFamily="sans-serif"
            android:text="Fawad's AI"
            android:textColor="#EEEAE2"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/lockStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center"
            android:text="Enter PIN or scan your face"
            android:textColor="#8A857D"
            android:textSize="13sp" />

        <!-- PIN dots -->
        <LinearLayout
            android:id="@+id/pinDots"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:orientation="horizontal">
            <!-- 4 dots created in code -->
        </LinearLayout>

        <Button
            android:id="@+id/scanFaceBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="22dp"
            android:background="@drawable/bg_save_button"
            android:paddingHorizontal="28dp"
            android:text="Unlock with Face"
            android:textColor="#1A1408"
            android:textStyle="bold" />

        <Button
            android:id="@+id/usePinBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@android:color/transparent"
            android:text="Use PIN instead"
            android:textColor="#8A857D"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- PIN keypad (hidden by default) -->
    <LinearLayout
        android:id="@+id/keypad"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="26dp"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="30dp"
        android:visibility="gone">

        <GridLayout
            android:id="@+id/keypadGrid"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:columnCount="3"
            android:rowCount="4" />
    </LinearLayout>
</FrameLayout>
